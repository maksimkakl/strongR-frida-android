name: strongR-frida

on:
  workflow_dispatch:

jobs:
  android_build:
    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: set up JDK 17
      uses: actions/setup-java@v3
      with:
        distribution: "temurin"
        java-version: "17"

    - name: Setup Android NDK
      id: setup-ndk
      uses: nttld/setup-ndk@v1.0.6
      with:
        ndk-version: r25b

    - name: Set up Python 3.9
      uses: actions/setup-python@v5.1.0
      with:
        python-version: '3.9'

    - name: install dependencies
      run: |
        sudo apt-get update && DEBIAN_FRONTEND=noninteractive sudo apt-get install build-essential tree ninja-build gcc-multilib g++-multilib lib32stdc++-9-dev flex bison ruby ruby-dev python3-requests python3-setuptools python3-dev python3-pip libc6-dev libc6-dev-i386 -y
        sudo gem install fpm -v 1.11.0 --no-document
        python3 -m pip install lief graphlib

    - name: Get Frida version
      id: fridaVersion
      run: |
        FRIDA_VERSION=$(curl -s https://api.github.com/repos/frida/frida/releases/latest | python3 -c "import sys,json; print(json.load(sys.stdin)['tag_name'])")
        echo "FRIDA_VERSION=$FRIDA_VERSION" >> $GITHUB_OUTPUT
        echo "Frida version: $FRIDA_VERSION"

    - name: build frida for Android
      shell: bash
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "actions@github.com"
        git clone https://github.com/hzzheyang/Patchs
        export ANDROID_NDK_ROOT=${{ steps.setup-ndk.outputs.ndk-path }}
        git clone --recurse-submodules https://github.com/frida/frida
        cd frida
        for path in ../Patchs/strongR-frida/*
        do
          name=$(basename $path)
          real=$(realpath $path)
          echo "Apply patches in $real to frida/subprojects/$name"
          cd subprojects/$name
          git am ../../../Patchs/strongR-frida/$name/*.patch
          cd ../..
        done
        cd ..

        # Собираем ТОЛЬКО под твой телефон (arm64), чтобы не тратить время и избежать ошибок путей
        mkdir build-android-arm64
        cd build-android-arm64
        ../frida/configure --host=android-arm64
        make
        cd ..

    - name: package build result for Android
      shell: bash
      run: |
        gzip build-android-arm64/subprojects/frida-core/server/frida-server

    - name: Create Release
      id: createRelease
      uses: actions/create-release@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: "${{ steps.fridaVersion.outputs.FRIDA_VERSION }}-${{ github.run_number }}"
        release_name: "${{ steps.fridaVersion.outputs.FRIDA_VERSION }} (strongR arm64)"
        prerelease: false

    - name: Upload android arm64 frida-server
      uses: actions/upload-release-asset@v1.0.2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: '${{ steps.createRelease.outputs.upload_url }}'
        asset_path: '${{ github.workspace }}/build-android-arm64/subprojects/frida-core/server/frida-server.gz'
        asset_name: 'hluda-server-${{ steps.fridaVersion.outputs.FRIDA_VERSION }}-android-arm64.gz'
        asset_content_type: application/octet-stream
